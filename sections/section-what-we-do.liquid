{% liquid
  assign section_id = section.id
  assign subtitle = section.settings.subtitle
  assign title = section.settings.title
  assign title_max_width_mobile = section.settings.title_max_width_mobile
  assign title_max_width_desktop = section.settings.title_max_width_desktop

  assign padding_top_mobile = section.settings.padding_top_mobile
  assign padding_bottom_mobile = section.settings.padding_bottom_mobile
  assign padding_top_desktop = section.settings.padding_top_desktop
  assign padding_bottom_desktop = section.settings.padding_bottom_desktop
%}

{% render 'offset',
  id: section_id,
  padding_top_mobile: padding_top_mobile,
  padding_bottom_mobile: padding_bottom_mobile,
  padding_top_desktop: padding_top_desktop,
  padding_bottom_desktop: padding_bottom_desktop
%}

{% style %}
    #section-{{ section_id }} .section-title {
    max-width: {% if title_max_width_mobile > 0 %} {{ title_max_width_mobile }}px {% else %} 100% {% endif %};

    @media screen and (min-width: 768px) {
      max-width: {% if title_max_width_desktop > 0 %} {{ title_max_width_desktop }}px {% else %} 100% {% endif %};
    }
  }
{% endstyle %}
<section id="section-{{ section_id }}">
  <div class="container">
    {% if subtitle != blank %}
      <div class="mb-2 text-center text-sm font-medium uppercase tracking-wide text-police-blue-70">{{ subtitle }}</div>
    {% endif %}
    {% if title != blank %}
      <div class="section-title mx-auto text-center text-32 font-medium lg:text-5xl">{{ title }}</div>
    {% endif %}
    <div class="mt-14 lg:grid lg:grid-cols-3 lg:border-t lg:border-police-blue-20 lg:pt-12">
      {% for block in section.blocks %}
        {% if block.type == 'grid_item' %}
          {% liquid
            assign image = block.settings.image
            assign title = block.settings.title
            assign description = block.settings.description
            assign url = block.settings.url
          %}
          <style>
            .grid-item-title-{{block.id}} {
              max-width: {% if block.settings.title_max_width_mobile > 0 %} {{ block.settings.title_max_width_mobile }}px {% else %} 100% {% endif %};
              @media screen and (min-width: 768px) {
                max-width: {% if block.settings.title_max_width_desktop > 0 %} {{ block.settings.title_max_width_desktop }}px {% else %} 100% {% endif %};
              }
            }
            .grid-item-text-{{block.id}} {
              max-width: {% if block.settings.description_max_width_mobile > 0 %} {{ block.settings.description_max_width_mobile }}px {% else %} 100% {% endif %};
              @media screen and (min-width: 768px) {
                max-width: {% if block.settings.description_max_width_desktop > 0 %} {{ block.settings.description_max_width_desktop }}px {% else %} 100% {% endif %};
              }
            }
          </style>
          <div class="border-t border-police-blue-20 pb-8 pt-10 lg:border-r lg:border-t-0 lg:last:border-r-0">
            {% render 'picture', image_mobile: image, image_desktop: image, class: 'h-20 w-auto mx-auto' %}
            <div class="grid-item-title-{{block.id}} mx-auto mb-2 mt-4 text-center text-40 font-medium tracking-tight lg:mb-7 lg:mt-12 lg:px-4 xl:px-0 xl:text-5xl">
              {%- if url != blank -%}
                <a class="hover:underline" href="{{ url }}" title="{{ title }}">
                  {{- title -}}
                </a>
              {%- else -%}
                {{- title -}}
              {%- endif -%}
            </div>
            <div class="grid-item-text-{{block.id}} mx-auto text-center lg:px-2 xl:px-0">{{- description -}}</div>
          </div>
        {% endif %}
      {% endfor %}
    </div>
  </div>
</section>

{% schema %}
  {
    "name": "What We Do",
    "settings": [
      {
        "type": "text",
        "id": "subtitle",
        "label": "Section Subtitle"
      },
      {
        "type": "text",
        "id": "title",
        "label": "Section Title"
      },
      {
        "type": "number",
        "id": "title_max_width_mobile",
        "label": "Title Max Width Mobile",
        "default": 0
      },
      {
        "type": "number",
        "id": "title_max_width_desktop",
        "label": "Title Max Width Desktop",
        "default": 0
      },
      {
        "type": "header",
        "content": "Paddings"
      },
      {
        "type": "number",
        "id": "padding_top_mobile",
        "label": "Padding Top Mobile (px)",
        "info": "Until 768px",
        "default": 0
      },
      {
        "type": "number",
        "id": "padding_bottom_mobile",
        "label": "Padding Bottom Mobile (px)",
        "info": "Until 768px",
        "default": 0
      },
      {
        "type": "number",
        "id": "padding_top_desktop",
        "label": "Padding Top Desktop (px)",
        "info": "From 768px",
        "default": 0
      },
      {
        "type": "number",
        "id": "padding_bottom_desktop",
        "label": "Padding Bottom Desktop (px)",
        "info": "From 768px",
        "default": 0
      }
    ],
    "blocks": [
      {
        "type": "grid_item",
        "name": "Grid Item",
        "settings": [
          {
            "type": "image_picker",
            "id": "image",
            "label": "Image"
          },
          {
            "type": "text",
            "id": "title",
            "label": "Title"
          },
          {
            "type": "url",
            "id": "url",
            "label": "URL"
          },
          {
            "type": "number",
            "id": "title_max_width_mobile",
            "label": "Title Max Width Mobile"
          },
          {
            "type": "number",
            "id": "title_max_width_desktop",
            "label": "Title Max Width Desktop"
          },
          {
            "type": "text",
            "id": "description",
            "label": "Description"
          },
          {
            "type": "number",
            "id": "description_max_width_mobile",
            "label": "Description Max Width Mobile"
          },
          {
            "type": "number",
            "id": "description_max_width_desktop",
            "label": "Description Max Width Desktop"
          }
        ]
      }
    ],
    "presets": [
      {
        "name": "What We Do"
      }
    ]
  }
{% endschema %}
